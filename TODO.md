v0.2.0:
feat: pagination - search results capped at 60, needs api.py + index.html
feat: nearest-neighbor ref suggestions - propose visually similar photos after enrollment

v0.3.0:
YOLO bounding boxes + multi-pet (one box per detected animal, CLIP classifies each crop)
Event-based triggering, when possible (blocked on Immich Workflows shipping)
Visual search using ref embeddings instead of text.